Book Info has been deployed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Services created:
{{- range $name, $svc := .Values.services }}
  - {{ $svc.serviceName }} (app: {{ $name }}) on port {{ $svc.port }}
{{- end }}

Open the product page locally:

  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ (index .Values.services "productpage").serviceName }} 9080:9080

Then visit http://localhost:9080/productpage
